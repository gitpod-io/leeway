{
	"name": "leeway",
	"build": {
		"context": "..",
		"dockerfile": "../.gitpod.Dockerfile"
	},
	"runArgs": [
		"--privileged",
		"--security-opt=seccomp=unconfined",
		"--network=host"
	],
	"containerUser": "root"
}